# TODO

## Chrome ウェブストアへの公開

いつか出す。現時点では未着手で、出す前に揃えるものがある。

### 用意できているもの

- [x] **アイコン** — `assets/icon.svg` が元図。`node scripts/make-icons.mjs` で
      16/32/48/128 の PNG を `public/icons/` へ書き出す。manifest 反映済み
      - ⚠ YouTube の Shorts ロゴは使っていない（商標。掲載を弾かれる要因になる）。
        縦長の動画に取り消し線、という一般的な形で描いてある
- [x] **スクリーンショット** — `node scripts/make-screenshots.mjs` で
      `assets/store/` に 1280×800 を3枚。設定画面と、拡張あり/なしの比較
- [x] **説明文・カテゴリ・単一用途・権限の正当化・データ申告**
      — すべて `assets/store/LISTING.md` に、貼れる形で置いてある

### まだ要るもの（人の手が要る）

- [ ] **開発者登録** — 初回のみ登録料。ダッシュボードでの手作業
- [ ] **スクリーンショットの目視** — ⚠ 実ページを撮っているので、実在の
      サムネイルとチャンネル名が写る。都合が悪いものが入っていないか見てから上げる
- [ ] **アイコンの見た目の確認** — 16px でも「縦長動画に取り消し線」と読めるか

### 出す前に確認したいこと

- [ ] **ログイン状態での動作確認** — ホームの Shorts 棚と再生ページの関連動画。
      検索ページとサイドバーは実測で確認済みだが、この2つは**ログアウトでは
      Shorts 自体が出ないため未検証**。選択子は構造からの推定にとどまる

### 出した後に効いてくること

⚠ 隠す機能は YouTube の内部の要素名（`ytd-reel-shelf-renderer`・
`ytm-shorts-lockup-view-model` 等）に依存する。YouTube はこれを変えるので、
**壊れたら直す前提**で持つこと。手元読み込みなら即直せるが、公開していると
審査待ちが挟まる。

## 動作確認の再現方法

YouTube の作りが変わって効かなくなったら、推測で選択子をいじる前に測る。
Playwright（グローバル導入済み）で実ページを開き、

1. `a[href^="/shorts/"]` の**祖先要素**を辿って、隠すべきコンテナ名を出す
2. その CSS を注入した前後で、**見えている Shorts の数**を数える
   （`offsetParent !== null` で判定する。画像を見て判断しない）

初版はこの方法で `ytm-shorts-lockup-view-model` の存在に気づいた。
